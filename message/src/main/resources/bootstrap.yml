
spring:
  application:
      name: message
  cloud:
    consul:
      host: 127.0.0.1
      port: 8500
      discovery:
        instance-id: ${spring.application.name}-${random.int}

    config:
      uri: http://127.0.0.1:8888
      fail-fast: true
      username: rrtx
      password: rrtx
      label: master
      profile: dev
      enabled: false
server:
  port: 0


management:
  security:
    enabled: false